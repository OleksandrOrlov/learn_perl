on: [push]

jobs:
  my_job:
    runs-on: ubuntu-latest
    name: My job
    container:
      image: registry.opensuse.org/home/oorlov/containers/containers/tumbleweed:openqa-tests-ci
    steps:
      - name: Get job status
        shell: bash
        env:
          API_KEY: ${{ secrets.API_KEY }}
          API_SECRET: ${{ secrets.API_SECRET }}
        run: |
          openqa-cli api --host http://openqa.opensuse.org --apikey=$API_KEY --apisecret=$API_SECRET jobs/1959258
          openqa-cli api -X POST --o3 --apikey=$API_KEY --apisecret=$API_SECRET job_templates_scheduling/74 schema=JobTemplates-01.yaml schema=JobTemplates-01.yaml preview=0 --param-file template=Job_Group_settings.yaml